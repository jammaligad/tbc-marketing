import { Fraunces, Inter } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  variable: "--font-sans",
});

export const fraunces = Fraunces({
  subsets: ["latin"],
  variable: "--font-serif",
});
